export class Movie {
    title: string;
    category: string;
    year: number;

    constructor(title: string, category: string, year: number) {
        this.title = title;
        this.category = category;
        this.year = year;
    }
}
